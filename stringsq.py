import pdb
# Duplicate characters in a str
# 

class StringsQ(object):
	@classmethod
	def getDuplicateChars(self,aStr):
		# Make sure type is str
		assert isinstance(aStr, str), "The value of arguement needs to be 'str' \t\ninstead its "+type(aStr)
		return "a"

StringsQ.getDuplicateChars("aStr")

